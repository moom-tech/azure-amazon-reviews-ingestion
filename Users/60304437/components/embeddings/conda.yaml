name: embedding_env
channels:
  - conda-forge
dependencies:
  - python=3.8
  - pandas
  - pyarrow
  - pip
  - pip:
      - sentence-transformers
      - torch --index-url https://download.pytorch.org/whl/cpu